name: aniket_pro_ai
description: ANIKET PRO AI - SMC Chart Analyzer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.7.0
  shared_preferences: ^2.2.3
  permission_handler: ^11.3.1
  flutter_overlay_window: ^0.4.3
  screen_capture_event: ^1.0.0
  gal: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
